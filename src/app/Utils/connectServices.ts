export const connectSpotify = async () => {
  //Here add all the APIs and connections needed to get the login and return the data that we need
  return;
};

export const connectNetflix = async () => {
  //Here add all the APIs and connections needed to get the login and return the data that we need
  return;
};

export const connectUberEats = async () => {
  //Here add all the APIs and connections needed to get the login and return the data that we need
  return;
};

export const connectStarbucks = async () => {
  //Here add all the APIs and connections needed to get the login and return the data that we need
  return;
};
